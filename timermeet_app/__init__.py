"""TimerMeet — native Python desktop app for Microsoft Teams meeting reminders.

Successor to the PHP/JS web app kept in ``legacy-php/`` (frozen at v1.3.0). The
web version's alerts depended on a browser tab staying open and focused; this
package runs as a plain OS process so alarms fire regardless of any browser.
"""

__version__ = "2.11.0"
